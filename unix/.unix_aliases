# terminal
alias ll="ls -alX"
alias lt="ls -ctalX"
alias c="clear"
alias gs="git status"
alias sm="bash ~/bash/run-menu.sh"
alias copy="xclip -selection clipboard"
alias z="eza -al"

# nvim
alias v="nvim"
alias n="nvim"
alias vim="nvim"

# python3
alias p="python3"

# change directory depending on file system
kb() {
  if [ -d /mnt/c/kb ]; then
    cd /mnt/c/kb || return
  elif [ -d /home/cj/git/kb ]; then
    cd /home/cj/git/kb || return
  else
    echo "kb directory not found." >&2
    return 1
  fi
}

# windows home
alias wh="/mnt/c"
# website
alias website="~/git/php-website"
# resume
alias resume="~/git/tex-resume"
# youtune-dl
alias youtube-dl='python3 /usr/local/bin/youtube-dl'

# export PATH
export PATH="$PATH:/opt/nvim/"
