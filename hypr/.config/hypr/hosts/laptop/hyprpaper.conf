# hyprpaper 0.8+ block syntax (the 0.7 preload/wallpaper lines are silently
# ignored). Path resolves through the ~/.config/hypr symlink into the repo.
wallpaper {
    monitor = eDP-1
    path = ~/.config/hypr/hosts/laptop/wallpaper.jpg
    fit_mode = cover
}
