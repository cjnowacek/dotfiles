{
    // Shared bar for both machines. The per-host part is only what hardware
    // dictates: "modules-right" and "hyprland/workspaces" live in host.jsonc
    // (a bootstrap-made symlink to hosts/<role>/host.jsonc). Keys defined
    // here win over the include, so keep those two OUT of this file.
    // Module definitions below are shared even when only one host lists
    // them (battery, custom/dropbox) — unlisted modules are inert.
    "include": "$HOME/.config/waybar/host.jsonc",

    "layer": "top",
    "position": "top",
    "height": 34,
    "spacing": 8,

    "modules-left": ["hyprland/workspaces", "hyprland/window"],
    "modules-center": ["clock"],

    "clock": {
        "format": "{:%a %b %d  %I:%M %p}",
        "format-alt": "{:%A, %B %d, %Y  %I:%M:%S %p}",
        "tooltip-format": "<tt><small>{calendar}</small></tt>",
        "calendar": {
            "mode": "month",
            "mode-mon-col": 3,
            "weeks-pos": "right",
            "on-scroll": 1,
            "format": {
                "months": "<span color='#89b4fa'><b>{}</b></span>",
                "weekdays": "<span color='#f9e2af'><b>{}</b></span>",
                "weeks": "<span color='#6c7086'>W{}</span>",
                "today": "<span color='#89b4fa' background='#313244'><b><u>{}</u></b></span>"
            }
        }
    },

    "hyprland/window": {
        "format": "{class}",
        "max-length": 50,
        "separate-outputs": true,
        "rewrite": {
            "kitty": " Kitty",
            "firefox": " Firefox",
            "chromium": " Chromium",
            "discord": "󰙯 Discord",
            "vesktop": "󰙯 Vesktop",
            "spotify": " Spotify",
            "code": "󰨞 Code",
            "thunar": " Files",
            "steam": " Steam",
            "obsidian": "󱓧 Obsidian",
            "unity": "󰛓 Unity",
            "blender": "󰂫 Blender",
            "(.*)": "$1"
        }
    },

    "pulseaudio": {
        "format": "{icon}  {volume}%",
        "format-muted": "  muted",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["", "", ""]
        },
        "on-click": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
        "on-click-right": "pavucontrol",
        "scroll-step": 5
    },

    "network": {
        "interval": 2,
        "format-ethernet": "  {ipaddr}  ↓{bandwidthDownBits} ↑{bandwidthUpBits}",
        "format-wifi": "  {signalStrength}%  ↓{bandwidthDownBits} ↑{bandwidthUpBits}",
        "format-disconnected": "  disconnected",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}\n{bandwidthUpBits} up / {bandwidthDownBits} down"
    },

    "cpu": {
        "format": "  {usage}%",
        "interval": 1,
        "on-click": "kitty -e btop"
    },

    "memory": {
        "format": "  {percentage}%",
        "interval": 2,
        "on-click": "kitty -e btop",
        "tooltip-format": "{used:0.1f}G / {total:0.1f}G"
    },

    "custom/dropbox": {
        "exec": "~/.config/waybar/scripts/dropbox-status.sh",
        "return-type": "json",
        "interval": 10,
        "on-click": "kitty -e yazi ~/Dropbox",
        "on-click-right": "systemctl --user restart rclone-dropbox.service"
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },

    "tray": {
        "icon-size": 16,
        "spacing": 8
    }
}
